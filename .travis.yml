language: go

env:
  - "GO111MODULE": "on"

go:
  - "1.12"
  - master

before_install:
  - go mod vendor
  - go build ./cmd/jacl

script:
- $HOME/gopath/bin/goveralls -service=travis-ci -ignore "parser"